// @flow
type ActionTypeMap = {
  HIDE_KEYBOARD: 'Keyboard/HIDE_KEYBOARD',
  KEYBOARD_HIDDEN: 'Keyboard/KEYBOARD_HIDDEN',
  KEYBOARD_SHOWN: 'Keyboard/KEYBOARD_SHOWN',
}
const actionTypes: ActionTypeMap = {
  HIDE_KEYBOARD: 'Keyboard/HIDE_KEYBOARD',
  KEYBOARD_HIDDEN: 'Keyboard/KEYBOARD_HIDDEN',
  KEYBOARD_SHOWN: 'Keyboard/KEYBOARD_SHOWN',
}
export {actionTypes}